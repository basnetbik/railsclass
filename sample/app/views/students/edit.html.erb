
<%= flash[:error] %>
<ul>
  <% @student.errors.full_messages.each do |msg| %>
    <li><%= msg %> </li>
  <% end %>
</ul>


<%= form_for( @student ) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "update" %>
<% end %>